# HTML Presenter — 発表者ツール

HTML のプレゼン資料に、PowerPoint の発表者ツール相当の機能を付ける単一ファイルのビューア。

## 使い方

1. `presenter.html` をダブルクリックして開く
2. 資料の HTML をドラッグ＆ドロップ（画像を外部参照している資料は**フォルダごと**）
3. 「🖥 別モニタで開く」→ 開いたウィンドウを発表用モニタへドラッグ
4. このツールに戻って `F`（または発表用ウィンドウで F11）で全画面
5. 以降の操作はすべて発表者ツール側で行う

## 対応する資料

| 種類 | 判定 | 送り方 |
|---|---|---|
| スライド型 | `section.slide` などで区切られている | ページ送り |
| スライド型（独自クラス） | 構造から自動検出 | ページ送り |
| スライド型（全画面section） | 全部見えていて各要素が画面1枚分 | スクロール |
| ドキュメント型 | 普通の縦長ページ | 見出し単位のジャンプ＋スクロール同期 |

判定を誤ったときは、上部の「区切り」プルダウンで手動変更できる。

## 機能

- **カンペ**：スライドごとに書ける。ブラウザに自動保存。`.notes.json` で書き出し／読み込み
- **次スライドのプレビュー**
- **タイマー**：経過時間／目標時間からの残り時間（超過で赤）
- **レーザーポインタ**：プレビュー上のマウスが発表用モニタに赤い光点として出る
- **スポットライト**：周囲を暗くして注目箇所だけ見せる（半径と暗さを調整可）
- **黒画面／白画面**
- **サムネ一覧**：クリックでジャンプ。カンペのあるスライドには印が付く
- **表示倍率**：本番画面と手元の発表者ツールで別々に指定できる

### レーザー／スポットライトの出し方

どちらも **キーを押している間だけ表示**され、離すと元に戻る。
出しっぱなしにしたいときは `Shift` を併用するか、ツールバーのボタンを押す。

| 操作 | 動き |
|---|---|
| `L` / `S` を押している間 | レーザー／スポットライト（離すと消える） |
| `Shift`+`L` / `Shift`+`S` | 出しっぱなしにする／戻す |
| ツールバーのボタン | 出しっぱなしにする／戻す |

スポットライトの明るい部分の広さは「半径」、周囲をどれだけ暗くするかは「暗さ」で調整する。
既定は 60%。設定はブラウザに保存され、次回も引き継がれる。

### 表示倍率

Chrome のズームは**オリジン単位**で効くため、`file://` で開いた発表者ツールと
スライドショーは、ブラウザの機能では別々の倍率にできない（`Ctrl` + `+` を押すと両方変わる）。
そこでツール側に倍率を持たせている。

- **本番**：スライドショー側の倍率。手元のプレビューにも同じ見え方が反映される（`+` `-` キーでも変更可）
- **手元**：この発表者ツール自体の倍率。本番の見え方は一切変わらない

「発表者ツールは 120%、資料は 160%」のような指定ができる。

## キー操作

`→ ↓ Space PageDown` 次へ / `← ↑ PageUp` 前へ / `Home` `End` 最初・最後
`G` 一覧 / `B` 黒 / `W` 白 / `F` 全画面
`L` `S` 押している間だけレーザー／スポット、`Shift+L` `Shift+S` で固定
`+` `-` 本番の表示倍率
`T` タイマー / `Shift+T` リセット / `R` 再読込 / `数字→Enter` 番号ジャンプ / `?` ヘルプ

マウス（左のプレビュー上）：左クリック＝次、右クリック＝前、ホイール＝送り／スクロール

## 仕組み

- 別ウィンドウは `about:blank` に資料を `document.write` する。親と同一オリジンになるため
  `file://` で開いても DOM を直接操作でき、postMessage 等を介さずに同期できる
- プレビューの iframe は**発表用ウィンドウと同じ論理サイズ**で描画し、CSS `transform: scale()`
  で縮小表示する。レイアウトが完全に一致するので、スクロール位置もマウス座標も 1:1 で対応する
- スライドの表示切替は、資料の CSS ルールを走査して「付けると表示になるクラス」
  （`.slide.active{display:flex}` の `active`）を推定し、元の仕組みにそのまま乗る
- 本番の表示倍率は資料の `body` に `zoom` を掛けて実現する。`documentElement` ではなく
  `body` に掛けることで、重ね物（レーザー等）の座標系を倍率で割るだけで揃えられる

## 制限

- 別ウィンドウを開くのにポップアップ許可が必要
- 外部 CDN の CSS を使う資料では、表示切替クラスの推定ができないことがある
  （その場合も display 直接指定で動作する）
